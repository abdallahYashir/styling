<%= form_with(model: schedule_type, local: true) do |form| %>
  <% if schedule_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(schedule_type.errors.count, "error") %> prohibited this schedule_type from being saved:</h2>

      <ul>
      <% schedule_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :schedule_type_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
